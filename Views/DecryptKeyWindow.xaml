<Window x:Class="BtcAddress.Views.DecryptKeyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Key Decrypter for Encrypted/Two-Factor Keys"
        Height="190"
        Width="744"
        MinHeight="190">
    <Grid Margin="12,10,12,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Enter an encrypted key" VerticalAlignment="Center" Margin="0,0,10,0" />
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtEncrypted" />

        <Label Grid.Row="1" Grid.Column="0" Content="Enter the passphrase&#x0a;or second factor" VerticalAlignment="Center" Margin="0,8,10,0" />
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtPassphrase" Margin="0,8,0,0" />

        <Button Grid.Row="2" Grid.Column="1" x:Name="btnDecrypt" Margin="0,8,0,0" Width="162" HorizontalAlignment="Left" Height="27" Content="Decrypt" Click="BtnDecrypt_Click" />
    </Grid>
</Window>



